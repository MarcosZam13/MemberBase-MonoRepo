// page.tsx — Página de registro de GymBase
// Usa el formulario propio para invocar la acción gymbase que usa el cliente admin
import { GymRegisterForm } from "@/components/shared/GymRegisterForm";

export default function RegisterPage() {
  return <GymRegisterForm />;
}
